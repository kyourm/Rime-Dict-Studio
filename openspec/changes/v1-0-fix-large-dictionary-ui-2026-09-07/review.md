# Verification and review

## Standards

初审发现渲染批量 Magic Number 及切换词典未重置窗口。提取统一常量并增加切换回归测试后，复审无 P1/P2。

## Spec

初审发现侧栏缺少视口高度约束，独立滚动不一定生效。应用外壳固定为视口高度、主区允许收缩、窄屏明确网格行高后，复审无 P1/P2。

## Diagnosis

修复前 5,000 条测试词典会创建 5,000 个词条 DOM；修复后 90,000 条测试词典仅创建 300 个首批 DOM，并可分批加载。根因是无上限 DOM 渲染，而不是文件读取或解析。
